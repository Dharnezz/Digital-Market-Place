# API base URL. Leave empty for local development — Vite proxies /api to
# http://localhost:8090. For production builds set this to the deployed backend,
# e.g. VITE_API_URL=https://digital-marketplace-api.onrender.com
VITE_API_URL=